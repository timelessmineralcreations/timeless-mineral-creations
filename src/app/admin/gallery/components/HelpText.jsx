export default function HelpText({ children }) {
  return (
    <span
      style={{
        color: "#87938e",
        fontSize: "12px",
        lineHeight: 1.45,
      }}
    >
      {children}
    </span>
  );
}